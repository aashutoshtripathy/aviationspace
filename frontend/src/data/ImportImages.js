// src/data/importImages.js
// const importAll = (r) => r.keys().map((key) => r(key).default);

// const images = importAll(require.context('../assets/coursal', false, /\.(png|jpe?g|svg)$/));

// export default images;
